# Git Common Command
```shell
git pull
git init
git add [somefile]
git ignore [somefile]
git checkout -b [newbranchname]
git add -u
git commit -m "[commont]"
git push origin [newbranchname]
(switch to website then send PullRequest)
git checkout master
```
